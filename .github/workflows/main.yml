name: cd

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v2
      - name: Build the image
        run: |
          echo ${{ secrets.GH_PAT }} | docker login ghcr.io --username ${{ secrets.GH_USER }} --password-stdin
          docker build --no-cache -t ghcr.io/${{ github.repository }}/cgc-cloud:latest
      - name: Publish the image
        run: |
          docker push ghcr.io/${{ github.repository }}/cgc-cloud:latest
